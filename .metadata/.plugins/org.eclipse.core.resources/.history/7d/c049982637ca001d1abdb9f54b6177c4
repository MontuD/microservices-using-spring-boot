package com.example.bookcatalogservice.repository;

import java.util.List;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.stereotype.Repository;

import com.example.bookcatalogservice.entity.Book;

@Repository
public interface BookRepository extends JpaRepository<Book, String> {

	@Query("SELECT u from BOOK u Where u.book_name = bookName")
	public List<Book> getBookByBookName(String bookName);
	
	@Query("SELECT u from BOOK u Where u.book_author = author")
	public List<Book> getBookByAuthor(String author);
	
}
