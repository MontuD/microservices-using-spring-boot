package com.example.userservice.userservice;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import com.example.services.userservice.repository.UserRepository;
import com.example.services.userservice.users.UserEntity;
import com.example.userservice.dto.UserDTO;

@Service
public class UserService {
	
	@Autowired
	private UserRepository userRepository;
	
	public UserEntity addUser(UserDTO userDTO) {
		UserEntity userEntity = new UserEntity(userDTO.fullname(),
				userDTO.email(),
				userDTO.password());
		userRepository.save(userEntity);
		return userEntity;
	}

}
